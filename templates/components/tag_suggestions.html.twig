<div class="py-1">
  {% for tag in tags %}
  <div
    class="px-3 py-2 hover:bg-gray-100 cursor-pointer text-sm tag-suggestion"
    data-tag-name="{{ tag.name }}"
  >
    {{ tag.name }}
  </div>
  {% endfor %}
</div>
