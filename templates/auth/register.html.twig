{% extends 'base.html.twig' %}

{% block title %}{{ 'app.title_register'|trans }}{% endblock %}

{% block body %}
<div class="min-h-screen py-12 px-4 sm:px-6 lg:px-8">
    <div class="flex justify-center">
        <div class="max-w-md w-full space-y-8">
        <div>
            <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
                {{ 'auth.register.heading'|trans }}
            </h2>
            <p class="mt-2 text-center text-sm text-gray-600">
                {{ 'auth.register.description'|trans }}
            </p>
        </div>

        <div class="bg-white py-8 px-6 shadow rounded-lg sm:px-10">
            {{ form_start(form, {
                'attr': {
                    'class': 'space-y-6',
                    'hx-post': path('app_register'),
                    'hx-target': '#response-area',
                    'hx-swap': 'innerHTML'
                }
            }) }}

                <div id="response-area">
                    {% for error in form.vars.errors %}
                        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
                            {{ error.message }}
                        </div>
                    {% endfor %}
                </div>

                <div>
                    {{ form_label(form.email, 'auth.register.email_label'|trans, {
                        'label_attr': {'class': 'block text-sm font-medium text-gray-700'}
                    }) }}
                    <div class="mt-1">
                        {{ form_widget(form.email, {
                            'attr': {
                                'class': 'appearance-none block w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm',
                                'placeholder': 'auth.register.email_placeholder'|trans
                            }
                        }) }}
                        {{ form_errors(form.email, {
                            'attr': {'class': 'mt-2 text-sm text-red-600 block'}
                        }) }}
                    </div>
                </div>

                <div>
                    {{ form_label(form.password.first, 'auth.register.password_label'|trans, {
                        'label_attr': {'class': 'block text-sm font-medium text-gray-700'}
                    }) }}
                    <div class="mt-1">
                        {{ form_widget(form.password.first, {
                            'attr': {
                                'class': 'appearance-none block w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm',
                                'placeholder': 'auth.register.password_placeholder'|trans
                            }
                        }) }}
                        {{ form_errors(form.password.first, {
                            'attr': {'class': 'mt-2 text-sm text-red-600 block'}
                        }) }}
                    </div>
                </div>

                <div>
                    {{ form_label(form.password.second, 'auth.register.confirm_password_label'|trans, {
                        'label_attr': {'class': 'block text-sm font-medium text-gray-700'}
                    }) }}
                    <div class="mt-1">
                        {{ form_widget(form.password.second, {
                            'attr': {
                                'class': 'appearance-none block w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm',
                                'placeholder': 'auth.register.confirm_password_placeholder'|trans
                            }
                        }) }}
                        {{ form_errors(form.password, {
                            'attr': {'class': 'mt-2 text-sm text-red-600 block'}
                        }) }}
                    </div>
                </div>

                <div>
                    <button type="submit"
                            class="group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                        {{ 'auth.register.submit'|trans }}
                    </button>
                </div>

                <div class="text-center">
                    <p class="text-sm text-gray-600">
                        {{ 'auth.register.have_account'|trans }}
                        <a href="{{ path('app_login') }}" class="font-medium text-indigo-600 hover:text-indigo-500">
                            {{ 'auth.register.login_link'|trans }}
                        </a>
                    </p>
                </div>

            {{ form_end(form) }}
        </div>
    </div>
    </div>
</div>
{% endblock %}
